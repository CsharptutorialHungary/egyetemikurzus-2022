@model TodoViewModel

<div class="row justify-content-center">
    <div class="col-xl-6 col-lg-8 col-md-8 col-sm-10">
        <table class="table">
            <thead>
                <tr class="table-primary">
                    <th scope="col">Azonosító</th>
                    <th scope="col">Teendő</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var t in Model.TodoList)
                {
                    <tr>
                        <th class="todos-column" scope="row">@t.Id</th>
                        <td class="todos-column">@t.Név</td>
                        <td class="buttons-column">
                            <input type="submit" class="btn btn-danger" value="Törlés" onClick="deleteTodo(@t.Id)" />
                            <input type="submit" class="btn btn-primary" value="Frissítés" onClick="populateForm(@t.Id)" />
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
